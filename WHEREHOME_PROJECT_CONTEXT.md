# 「在哪儿」项目上下文

最后更新：2026-08-16

## 这是什么

一个搜索「家里的东西放在哪儿」的移动优先 PWA。物品库由家里 29 张现状照片
识别生成，每件物品配一张从原图裁出来的小图，所以搜到之后能直接看出是哪个东西。

部署方式完全照搬同一作者的 NutriFlow（`../../eat/nutriflow-pwa`）：单文件 PWA 发
GitHub Pages，可选的云同步是独立的 Cloudflare Worker + D1。

## 当前状态

- 物品库：**1054 条**，其中 **778 条可搜索**。下架的 276 条分三种：家具本体与固定设备
  178 条、跨照片重复 80 条、经核对判定不是物品 18 条（`on:false`，数据全部保留）。
- 覆盖 9 个房间：厨房 242、客餐厅 221、主卧 104、南阳台 67、卫生间 46、次卧 29、
  玄关 27、洗漱区 21、北阳台 21。
- 17 个分类，最大的几个：衣物鞋包 156、包装废弃 99、收纳容器 91、洗漱护肤 77。
- 已实现：搜索、房间/分类筛选、图/列表两种视图、详情页、编辑（改名/改位置/改分类/
  改数量/备注）、删除、新增（可拍照）、深浅色切换、本地持久化、导出修改、云同步的
  上传/下载。
- 测试：`npm test`，7 个用例全绿。
- 部署：代码在 GitHub public 仓库；App 和数据在 Cloudflare Worker（`gate/`）后面，用密码 + 签名 cookie 保护。

## 数据是怎么来的

原始素材在上一级目录：`../IMG_8666.heic` – `../IMG_8694.heic`（29 张，iPhone 16 Pro，
最高 8064px）。生成脚本在会话的 scratchpad 里，关键步骤：

1. **转码**：HEIC → 全分辨率 JPEG。
2. **切片**：每张切成 2×2 象限（10% 重叠）+ 一张全景，象限最长边 1250px。
   全部用 PIL 完成，并逐块与记录的坐标范围比对校验。
3. **叠网格**：每个象限图上叠 6×6 网格，格子左上角印红色编号 A1–F6。
4. **识别**：每张照片派一个 agent，读全景 + 4 张带网格的象限图，穷举列出物品，
   每件给出 `tile`（哪个象限）+ `cells`（占哪几格，如 `B3:C5`）。
5. **裁图**：把格子范围换算回原图坐标，外扩 18%，最小边长不低于原图短边的 5.5%，
   用 PIL 裁出 420px 小图。
6. **过滤**：按规则剔除家具本体（整个 `家具大件` 分类）和固定设备（关键词命中：
   水槽、马桶、油烟机、壁挂炉、开关面板、柜门、大家电等），标 `on:false`。
7. **清洗**：对 5 组「同一地点拍了两张」的照片做跨照片去重（识别出 80 组重复），
   对 157 条低置信度条目逐张对着小图核对名称（改名 57 条、判定非物品 18 条、
   确认看不清 36 条）。

### 踩过的坑（重要）

- **`sips --cropOffset` 在左偏移为 0 时会静默失效**，退化成居中裁剪。这导致每张照片
  左半边的两块象限内容与记录坐标不符，裁出来全是墙面和空白。整条链路改用 PIL 后解决。
  诊断方法：把 manifest 里记录的坐标范围重新裁一次，和实际瓦片文件做像素差比对。
- **不要让模型输出归一化 bbox**。改成在图上叠编号网格、让它回答格子编号之后，定位
  准确率大幅提升。这是把回归问题换成了识别问题。
- **每个 agent 的读图量必须控制**。最早给每个 agent 2 张照片 × 9 张图，agent 在输出
  结果前就撑爆上下文，32 次启动 0 个完成。降到 1 张照片 × 5 张图后正常。
- **跑批前确认机器不休眠**。休眠会让 agent 大面积 stall，6 次重试各 180 秒，一轮
  5.5 小时且全部失败。用 `caffeinate` 时必须让它在后台真正存活（不能用 `&` 挂在
  一次性 shell 调用里）。

## 部署

App 走 GitHub Pages（和 NutriFlow、prettier 同一套），数据加密后一起放公开仓库。

| | 放哪 | 别人拿到能看懂吗 |
|---|---|---|
| 代码、图标、文档 | GitHub public 仓库 | 能，本来就该公开 |
| `public/data.enc` | GitHub public 仓库 | **不能**，AES-256-GCM 密文 |
| `public/items/`、`items_sm/`、`items.json` | 只在本地，`.gitignore` 排除 | 不上传 |

### 为什么是这个方案

- Cloudflare Pages + Access：Access 的 Self-hosted 应用要从账号下的域名选主机名，
  该账号没有域名，`*.pages.dev` 不属于用户，选不了。
- Cloudflare Worker（`gate/`，已部署但停用）：`*.workers.dev` 在国内被 DNS 污染，
  解析到无关 IP，不挂代理打不开。GitHub Pages 则国内直连正常。
- 所以最终：**用可达的 GitHub Pages 托管，用加密解决隐私**。

### 加密数据包

`scripts/build-encrypted.mjs` 把 items.json + 1054 张 300px 小图打成一个二进制容器再加密：

```
"WHE2" | iterations(4) | salt(16) | iv(12) | AES-256-GCM 密文
明文： metaLen(4) | metaJSON | 所有图片的原始 JPEG 字节
```

- 图片**不做 base64**（省 34% 体积），客户端解密后按 `off/len` 切成 Blob，
  由浏览器管理内存，不把十几 MB 字符串堆在 JS 堆上。
- PBKDF2-SHA256 600,000 轮（OWASP 建议值）+ AES-256-GCM。
- 客户端解密一次后把明文存进 IndexedDB，之后打开不用再输口令；
  设置页有「清除并锁定」。

⚠️ **密文是公开的，别人可以下载后离线暴力破解，所以口令必须够长。**
脚本强制 12 位以上。换口令：

```bash
node scripts/build-encrypted.mjs '新的长口令'
git add public/data.enc && git commit -m "换口令" && npm run publish:pages
```

换口令后，已解锁的设备因为缓存了明文不受影响；新设备要用新口令。

### 发布

```bash
npm test
npm run publish:pages     # 校验 → 推 main → 把 public/ 发到 gh-pages → 触发重建
```

站点：`https://wang-piaoliang.github.io/wherehome/`

### 云同步（可选，两人的修改互通）

`api/` 是独立 Worker + D1，只同步修改补丁（几十 KB）。注意 `*.workers.dev`
国内需要代理，所以这个功能在国内网络下不可用——两人互通目前只能靠手动导出/导入。

## 已知问题 / 待办

- **名称准确率**：名字是模型认的，会有错。低置信度的在界面上标了「存疑」，优先核对。
- **重复条目**：有 5 组照片拍的是同一个地方（卫生间镜柜、厨房吊柜、客餐厅边柜、
  客餐厅挂衣杆角、南阳台窗边），同一件实物会出现两次。已做过一轮自动去重，可能有残留。
- **同义词搜不到**：搜「行李箱」是 0 条，因为它被记成了「深灰色大容量旅行行李袋」；
  搜「行李」才能找到。后续可以加一个同义词表或做模糊匹配。
- 分类里「包装废弃」有 99 条（纸箱、吊牌、塑料袋）。用户明确要求保留。
- 尚未做：按容器（柜子）浏览、批量编辑、把用户修改回流进 items.json 的工具。

## 变更记录

- 2026-08-16：项目建立。完成识别流水线、App v1（搜索/浏览/编辑/新增/同步）、
  测试、文档。数据清洗后 1054 条、778 条可搜索。未发布。
  修掉两个 bug：网格卡片缩略图被 flex 拉伸；搜索把照片级的 `scene` 也纳入了检索，
  导致搜「空气炸锅」返回整个厨房 71 条。
